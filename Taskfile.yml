version: '3'

includes:
  backend:
    taskfile: ./backend
    dir: ./backend
  frontend:
    taskfile: ./frontend
    dir: ./frontend

tasks:
  docker:build:
    desc: Build both frontend and backend Docker images
    cmds:
      - docker build -t leszmonitor .

  docker:up:
    desc: Start all services using Docker Compose
    cmds:
      - docker compose up

  docker:down:
    desc: Stop all services using Docker Compose
    cmds:
      - docker compose down

  # Combined tasks
  dev:
    desc: Run both frontend and backend in development mode
    deps: [ backend:dev, frontend:dev ]

  build:
    desc: Build both frontend and backend
    deps: [ backend:build, frontend:build ]

  test:
    desc: Run tests for both frontend and backend
    deps: [ backend:test, frontend:test ]

  clean:
    desc: Clean all build artifacts
    deps: [ frontend:clean, backend:clean, logs:clean ]

  logs:clean:
    desc: Remove logs
    cmds:
      - rm -rf ./.dev-logs/* || true
      - rm -rf ./backend/.dev-logs/* || true
      - rm -rf ./.logs/* || true
      - rm -rf ./backend/logs/* || true
